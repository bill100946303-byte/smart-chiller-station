# V1.8 签收结论（主控）

## 1. 本轮结论
- 结论：**通过（PASS）**
- 判定时间：2026-03-12
- 判定依据：`AUTO_BOOT=1 SITE_ID=126lnoffice scripts/check_stack.sh`

## 2. 关键结果
- `sourceStatus overall`：`overview=ok`、`trends=ok`、`anomalies=ok`、`recommendations=ok`
- Readiness：`PASS`
- Dashboard：`M1 0/4 PASS`，`M2 0 PASS`，`M3 3/3 (100.0%) PASS`
- SystemOverview：`M1 0/1 PASS`，`M2 0 PASS`，`M3 3/3 (100.0%) PASS`
- `ND failed: 0`

## 3. 本轮主控改动（代码）
- 文件：`apps/chiller-bff/src/adapters/legacyEnergyAdapter.js`
- 改动点：
  1. 新增 `homeData/energyEfficiency` 快照回退解析，用于补齐 `station_cop`、`totalPowerKw`、温差核心量。
  2. 在 `loadEnergyOverview` 中为核心指标增加快照回退。
  3. 在 `loadTrendSeries` 中为 `currentCop` 增加快照单点回退（含时间戳）。
  4. 当 `runParams=500` 但回退已覆盖关键指标时，趋势来源状态标记为可用（不再把整页判为降级）。

## 4. 边界声明
- 未修改规则阈值。
- 未修改 `ruleId`。
- 未放宽 OpenAPI 主合同。
- 仅增强数据回退链路与来源状态判定。

## 5. 建议下一步
1. 冻结当前为 `V1.8 baseline`，补一组 UI 金样（PASS 态）截图。
2. 统一使用 `ui-badge-state-v1.8.json` 作为运行态状态文件，避免版本语义混淆。
3. 进入下一阶段：P1 规则可解释性与跨站点可比性优化。

## 6. 角标判定约束（签收补充）
- 页面角标只读后端状态文件（`docs/ui-badge-state-v1.8.json`）中的 `pass + text`。
- 前端不允许依据 KPI、趋势或诊断明细自行推断 PASS/FAIL。
- UI 取证文档：`docs/ui-consistency-review-v1.8.md`（PASS 态金样 24 张）。

## 7. 配套签收附件（线程产物）
- UI 发布选图（含运行态受限三语说明）：`docs/ui-release-visual-selection-v1.8.md`
- HVAC 签收文案包（v1.2.1）：`docs/hvac-signoff-copybook-v1.2.1.md`、`docs/hvac-signoff-copybook-v1.2.1.json`
- Preflight 门禁语义说明：`docs/preflight-gate-semantics-v1.0.md`
- Week1 运行巡检：`docs/readiness-week1-daily-checks-v1.0.csv`、`docs/readiness-week1-weekly-report-v1.0.md`

## 8. V19.2 主控统一状态
- 汇总文档：`docs/v19.2-master-status.md`
- 判定规则：发布放行以 `check_stack.sh` readiness 为主；`check:contract` 内 `runtime-ready` 仅作 non-blocking 诊断。
