# V1.6 签收结论（主控）

更新时间：2026-03-12

## 结论
- `V1.6` 当前 **不签收（No-Go）**。
- 依据：`non-degraded-readiness` 自动判定为 `FAIL`，归因 `upstream_interface`。

## 证据
- 自动判定报告：`docs/non-degraded-readiness-report-v1.6.json`
- 角标状态产物：`docs/ui-badge-state-v1.6.json`
- 栈检查：`scripts/check_stack.sh`（已集成 readiness + badge state 生成）

## 当前阻塞（按 owner）
1. `legacy`
   - `station_cop` 数据仍不可用，导致 `metric.station_cop` 健康检查失败。
   - `currentCop` 趋势序列无有效点，`M3` 无法达标。
2. `bff`
   - 已完成 fallback 与可观测性；`M2` 已恢复通过（freshness 不再被历史事件时间误伤）。
   - 当前阻塞主要来自上游数据质量，不是聚合逻辑错误。
3. `shell`
   - 角标状态读取链路已具备（后端产物已生成），待接入页面展示。

## 复测门槛（保持不变）
- Dashboard：`M1<=0/4`、`M2=0`、`M3>=80%` 且 `non_empty_series>=2`
- SystemOverview：`M1<=0/1`、`M2=0`、`M3>=80%`
- 连续两次采样（间隔 >=5 分钟）同时满足，方可签收。
