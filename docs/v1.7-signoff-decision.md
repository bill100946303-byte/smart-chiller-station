# V1.7 签收结论（主控）

## 1. 本轮结论
- 结论：**不签收（FAIL）**
- 当前阶段：**V1 工程化收口阶段（已完成 UI/合同/规则文案主链路，卡在真实链路非降级放行）**
- 判定口径：以 `non-degraded` 为唯一放行口径，不允许前端自行推断状态。

## 2. 本轮已完成（可冻结）
- UI 一致性终验产出完成：`ui-consistency-review-v1.7.md` + 16 张截图。
- BFF 合同门禁通过：`npm run check:contract` 持续通过，主合同未放宽。
- 合同探针增强完成：新增结构化 `real-link-ready-probe-report.json`，支持分组归因。
- HVAC 文案 v1.3 完成，且 `riskBoundaryCheck=4/4` 通过，保持 `ruleId/阈值/evaluator` 不变。

## 3. 阻塞项（本轮必须保留为 FAIL）
- 阻塞 1（P0）：`SystemOverview` 1366 正常态无法达到“全正常”。
  - 直接原因：`recommendations.sourceStatus=partial`
  - 关键缺口：`metric.station_cop` 缺失
- 阻塞 2（P0）：non-degraded 指标未达阈值（当前实时链路）。
  - Dashboard：`M1=2/4 FAIL`，`M2=0 PASS`，`M3=2/3 FAIL`
  - SystemOverview：`M1=0/1 PASS`，`M2=0 PASS`，`M3=2/3 FAIL`
- 阻塞 3（P1）：readiness 清单与展示字典存在门禁缺口（blocking=4，warning=8）。

## 4. 归因（责任边界）
- 主归因：`upstream_interface`（非前端渲染问题）。
- 探针分组（本轮）：
  - `upstream_unreachable=13`
  - `upstream_5xx=4`
  - `field_missing_or_invalid=1`
- 端点聚类（本轮）：
  - `/dashboard/overview=2`
  - `/dashboard/trends=2`
  - `/anomalies/summary=2`
  - `/recommendations=12`

## 5. 放行条件（进入 V1.8 通过的硬门槛）
- 条件 A：`SystemOverview` 与 `Dashboard` 同口径达到非降级阈值（连续两次，间隔 >=5 分钟）。
- 条件 B：`metric.station_cop` 进入可评估态（非 skip），`recommendations.sourceStatus.overall=ok`。
- 条件 C：`non-degraded-readiness-checklist` 的 blocking 项清零。

## 6. 当前建议
- 继续按“线程并行 + 主控关口”推进：
  1. 先收敛 `station_cop` 来源可用性与门禁；
  2. 再做真实链路复验；
  3. 最后再执行签收。
