# PHASE2_2D3D_INTERACTION_APPROVAL_REQUEST_V1

## 1. 本次申请概览

- 申请日期：2026-03-24
- 申请人：主控
- 审批人：项目负责人
- 是否批准进入 Phase 2：
  - `yes`

## 2. 本次仅申请的一条主线

本次选择：

- `2D/3D` 深交互增强

## 3. 本次目标

- 目标 1：在已签收 `/scene-control` 基础上，补强“场景 -> 设备 -> 场景”的深链接与上下文联动。
- 目标 2：在不重写 legacy 2D/3D 引擎的前提下，为场景页补一个最小可用的节点级交互承接层。
- 目标 3：将本次增强限定为“已签收能力增强”，不扩展为新的主业务域。

## 4. 本次明确不做项

- 不做项 1：不重写 legacy 2D/3D 引擎或静态资源结构。
- 不做项 2：不启动 `simulate`、`assistant`、正式 optimize engine。
- 不做项 3：不新增新的主业务域或大型分析专题页。
- 不做项 4：不把节点级控制下发做成第一批交付。

## 5. 实施边界

- 是否进主合同：
  - `yes`
- 是否新增页面：
  - `no`
- 是否只做治理不做实现：
  - `no`
- 是否涉及 legacy 改动：
  - `no`
- 是否涉及已签收页面重构：
  - `no`

补充边界说明：

- 本次只允许增强 `/scene-control` 与 `/devices` 现有联动能力。
- 允许补充最小 BFF 合同或聚合字段，但只服务于当前场景深交互增强。
- 不把场景页变成新的独立控制平台。

## 6. 责任归属

- 主控负责人：主控
- BFF 负责人：BFF 线程 / 主控统一收口
- UI 负责人：UI 线程 / 主控统一收口
- 文档/签收负责人：主控

## 7. 验收口径

- 成功标准 1：`/scene-control` 能稳定承接“楼层 + 设备 + 模式”深链接，并能可靠跳转到 `/devices` 保持上下文。
- 成功标准 2：场景页至少形成一项真实可用的节点级交互承接能力，而不只是 iframe 外壳展示。
- 成功标准 3：增强后不破坏当前 `/scene-control`、`/devices` 已签收状态，且运行链与主合同仍稳定。

## 8. 风险与回退

- 主要风险：
  - legacy 2D/3D 资产可交互能力不足
  - 设备与场景节点映射关系不完整
  - 深交互一旦越界，容易滑向旧引擎重写
- 若未通过审批或中途停止，回退方式：
  - 保持当前 `/scene-control` 已签收外壳态
  - 撤回新加的交互承接层，不影响原有 iframe 与设备上下文
- 是否会影响当前已签收页面稳定性：
  - `yes`

补充说明：

- `yes` 的含义是：本次增强会触及已签收 `/scene-control` 和 `/devices`，因此必须以小步增量和主控收口方式推进。

## 9. 审批结论

- 审批结果：
  - `批准`
- 批准范围：
  - 仅批准 `2D/3D` 深交互增强这一条 Phase 2 主线
- 附加限制：
  - 一次只做第一批最小增强，不得并发启动 `/simulate`、`/assistant` 或正式 optimize engine
  - 优先做“联动增强”，不做“引擎重写”
- 下次复核时间：
  - 第一批任务包完成后

## 10. 当前结论

本次申请已满足当前 Phase 2 开工门要求。

后续仅按这一条线推进：

- `2D/3D` 深交互增强

其他 Phase 2 能力继续保持：

- `backlog-only`
