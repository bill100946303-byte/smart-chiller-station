openapi: 3.0.3
info:
  title: Chiller BFF API
  version: 1.0.1
  description: |
    Contract for Chiller 2.0 BFF aggregation APIs.
    P0 routes are fully specified for FE/BE integration:
    - dashboard/overview
    - dashboard/trends
    - anomalies/summary
    - anomalies/list
servers:
  - url: http://127.0.0.1:8787
tags:
  - name: Health
  - name: Dashboard
  - name: Anomalies
  - name: Devices
  - name: System
  - name: Recommendations
  - name: Optimize
paths:
  /healthz:
    get:
      tags: [Health]
      summary: Health check
      operationId: getHealthz
      responses:
        "200":
          description: Service health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthzResponse"
        "500":
          $ref: "#/components/responses/InternalError"

  /bff/v1/sites/{siteId}/dashboard/overview:
    get:
      tags: [Dashboard]
      summary: Dashboard overview aggregate
      operationId: getDashboardOverview
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
      responses:
        "200":
          description: Dashboard overview payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardOverviewResponse"
              examples:
                default:
                  summary: P0 dashboard overview example
                  externalValue: ./examples/dashboard-overview.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/dashboard/trends:
    get:
      tags: [Dashboard]
      summary: Dashboard trend series aggregate
      operationId: getDashboardTrends
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - $ref: "#/components/parameters/RangeQueryParam"
      responses:
        "200":
          description: Dashboard trends payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardTrendsResponse"
              examples:
                default:
                  summary: P0 dashboard trends example
                  externalValue: ./examples/dashboard-trends.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/anomalies/summary:
    get:
      tags: [Anomalies]
      summary: Anomaly summary aggregate
      operationId: getAnomaliesSummary
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
      responses:
        "200":
          description: Anomalies summary payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnomaliesSummaryResponse"
              examples:
                default:
                  summary: P0 anomalies summary example
                  externalValue: ./examples/anomalies-summary.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/anomalies/list:
    get:
      tags: [Anomalies]
      summary: Anomaly list aggregate
      operationId: getAnomaliesList
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - name: page
          in: query
          required: false
          description: 1-based page index.
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: pageSize
          in: query
          required: false
          description: Number of rows per page.
          schema:
            type: integer
            minimum: 1
            default: 20
        - name: severity
          in: query
          required: false
          description: Severity filter for the final alarm taxonomy.
          schema:
            type: string
            enum: [critical, major, minor, normal]
        - name: state
          in: query
          required: false
          description: Optional legacy alarm state filter.
          schema:
            type: string
      responses:
        "200":
          description: Anomalies list payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnomaliesListResponse"
              examples:
                default:
                  summary: Sprint1 anomalies list example
                  externalValue: ./examples/anomalies-list.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/system/topology:
    get:
      tags: [System]
      summary: Static system topology aggregate
      operationId: getSystemTopology
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
      responses:
        "200":
          description: System topology payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemTopologyResponse"
              examples:
                default:
                  summary: System topology example
                  externalValue: ./examples/system-topology.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/system/diagram:
    get:
      tags: [System]
      summary: System diagram aggregate
      operationId: getSystemDiagram
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - $ref: "#/components/parameters/LayoutModeQueryParam"
        - $ref: "#/components/parameters/ScopeQueryParam"
      responses:
        "200":
          description: System diagram payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemDiagramResponse"
              examples:
                default:
                  summary: System diagram example
                  externalValue: ./examples/system-diagram.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/devices/list:
    get:
      tags: [Devices]
      summary: Device list aggregate
      operationId: getDevicesList
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - name: page
          in: query
          required: false
          description: 1-based page index.
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: pageSize
          in: query
          required: false
          description: Number of rows per page.
          schema:
            type: integer
            minimum: 1
            default: 12
        - name: type
          in: query
          required: false
          description: Optional system type filter.
          schema:
            type: string
            enum: [chiller, chilledPump, coolingPump, coolingTower, other]
        - name: floor
          in: query
          required: false
          description: Optional floor filter.
          schema:
            type: string
      responses:
        "200":
          description: Device list payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevicesListResponse"
              examples:
                default:
                  summary: Sprint1 devices list example
                  externalValue: ./examples/devices-list.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/devices/tree:
    get:
      tags: [Devices]
      summary: Device tree aggregate
      operationId: getDevicesTree
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - name: build
          in: query
          required: false
          description: Legacy building selector for tree source.
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: floor
          in: query
          required: false
          description: Legacy floor selector for tree source.
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        "200":
          description: Device tree payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevicesTreeResponse"
              examples:
                default:
                  summary: Sprint2 devices tree example
                  externalValue: ./examples/devices-tree.json
                degraded:
                  summary: Sprint2 devices tree degraded example
                  externalValue: ./examples/devices-tree-degraded.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/devices/{deviceId}:
    get:
      tags: [Devices]
      summary: Device detail aggregate
      operationId: getDeviceDetail
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - name: deviceId
          in: path
          required: true
          description: Device identifier from devices/list or devices/tree.
          schema:
            type: string
            minLength: 1
        - name: build
          in: query
          required: false
          description: Legacy building selector for detail tree source.
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: floor
          in: query
          required: false
          description: Legacy floor selector for detail tree source.
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        "200":
          description: Device detail payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceDetailResponse"
              examples:
                default:
                  summary: Sprint2 device detail example
                  externalValue: ./examples/device-detail.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/recommendations:
    get:
      tags: [Recommendations]
      summary: Recommendation cards aggregate
      operationId: getRecommendations
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
      responses:
        "200":
          description: Recommendations payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecommendationsResponse"
              examples:
                default:
                  summary: Recommendations example
                  externalValue: ./examples/recommendations.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/optimize:
    post:
      tags: [Optimize]
      summary: Build tower approach and pump delta-T AI advisor result
      operationId: postOptimizeDraft
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OptimizeDraftRequest"
      responses:
        "200":
          description: Tower approach and pump delta-T AI advisor result generated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptimizeAdvisorResponse"
              examples:
                default:
                  summary: Tower approach and pump delta-T AI advisor result example
                  externalValue: ./examples/optimize-advisor-result.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /bff/v1/sites/{siteId}/optimize/tower-approach/advice:
    post:
      tags: [Optimize]
      summary: Build tower approach AI target advice
      operationId: postTowerApproachAdvice
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OptimizeDraftRequest"
      responses:
        "200":
          description: Tower approach AI target advice generated.
          content:
            application/json:
              schema:
                type: object
                required: [ok, code, requestId, advisorResult, towerApproachAdvisor, details]
                properties:
                  ok:
                    type: boolean
                    enum: [true]
                  code:
                    type: string
                    enum: [OK]
                  requestId:
                    type: string
                  advisorResult:
                    type: object
                    nullable: true
                  towerApproachAdvisor:
                    type: object
                    nullable: true
                  details:
                    $ref: "#/components/schemas/OptimizeDraftDetails"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /bff/v1/sites/{siteId}/optimize/chiller-staging/samples:
    get:
      tags: [Optimize]
      summary: List append-only chiller staging samples
      operationId: listChillerStagingSamples
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - name: combinationKey
          in: query
          required: false
          schema:
            type: string
          description: Optional normalized combination key, such as CH4+CH5+CH7.
        - name: combination
          in: query
          required: false
          schema:
            type: string
          description: Optional raw combination text used when combinationKey is omitted.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 2000
            default: 100
      responses:
        "200":
          description: Append-only chiller staging sample list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChillerStagingSampleListResponse"
        "500":
          $ref: "#/components/responses/InternalError"

  /bff/v1/sites/{siteId}/optimize/executions:
    get:
      tags: [Optimize]
      summary: List governance optimize executions
      operationId: listOptimizeExecutions
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - $ref: "#/components/parameters/OptimizeExecutionStatusQueryParam"
        - $ref: "#/components/parameters/OptimizeExecutionLimitQueryParam"
      responses:
        "200":
          description: Governance optimize execution list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptimizeExecutionListResponse"
              examples:
                default:
                  summary: Optimize execution list example
                  externalValue: ./examples/optimize-executions-list.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Optimize]
      summary: Create governance optimize execution draft
      operationId: createOptimizeExecution
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOptimizeExecutionRequest"
      responses:
        "201":
          description: Governance optimize execution created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptimizeExecutionMutationResponse"
              examples:
                default:
                  summary: Optimize execution create example
                  externalValue: ./examples/optimize-executions-create.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /bff/v1/sites/{siteId}/optimize/executions/{executionId}/dispatch:
    post:
      tags: [Optimize]
      summary: Dispatch approved tower approach or pump delta-T execution by configured mode
      operationId: dispatchOptimizeExecution
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Optimize execution dispatch receipt persisted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptimizeExecutionMutationResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"
        "502":
          $ref: "#/components/responses/UpstreamError"

  /bff/v1/sites/{siteId}/optimize/executions/{executionId}/approve:
    post:
      tags: [Optimize]
      summary: Approve governance optimize execution
      operationId: approveOptimizeExecution
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveOptimizeExecutionRequest"
      responses:
        "200":
          description: Governance optimize execution approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptimizeExecutionMutationResponse"
              examples:
                default:
                  summary: Optimize execution approve example
                  externalValue: ./examples/optimize-executions-approve.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"

  /bff/v1/sites/{siteId}/optimize/executions/{executionId}/rollback:
    post:
      tags: [Optimize]
      summary: Roll back governance optimize execution
      operationId: rollbackOptimizeExecution
      parameters:
        - $ref: "#/components/parameters/SiteIdPathParam"
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RollbackOptimizeExecutionRequest"
      responses:
        "200":
          description: Governance optimize execution rolled back
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptimizeExecutionMutationResponse"
              examples:
                default:
                  summary: Optimize execution rollback example
                  externalValue: ./examples/optimize-executions-rollback.json
        "400":
          $ref: "#/components/responses/BadRequestError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"

components:
  parameters:
    SiteIdPathParam:
      name: siteId
      in: path
      required: true
      description: Site identifier, same as legacy dbName, e.g. 126lnoffice.
      schema:
        type: string
        minLength: 1
    RangeQueryParam:
      name: range
      in: query
      required: false
      description: Time range key for trends.
      schema:
        type: string
        enum: [24h, 7d, 30d]
        default: 24h
    LayoutModeQueryParam:
      name: layoutMode
      in: query
      required: false
      description: Layout mode for system diagram rendering.
      schema:
        type: string
        enum: [auto, fixed]
        default: auto
    ScopeQueryParam:
      name: scope
      in: query
      required: false
      description: Diagram scope level.
      schema:
        type: string
        enum: [main_loop, full]
        default: full
    OptimizeExecutionStatusQueryParam:
      name: status
      in: query
      required: false
      description: Optional governance execution status filter.
      schema:
        type: string
        enum: [pending_approval, approved, rolled_back]
    OptimizeExecutionLimitQueryParam:
      name: limit
      in: query
      required: false
      description: Maximum number of execution rows to return.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    ExecutionIdPathParam:
      name: executionId
      in: path
      required: true
      description: Governance optimize execution identifier.
      schema:
        type: string
        minLength: 1

  responses:
    BadRequestError:
      description: Invalid request parameter or malformed query.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            ok: false
            code: BAD_REQUEST
            error: "Invalid range value: 90d"
            requestId: "req-20260311-0001"
            details:
              field: range
    NotFoundError:
      description: Site not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            ok: false
            code: NOT_FOUND
            error: "Site not found: missing-site"
            requestId: "req-20260311-0002"
    ForbiddenError:
      description: Caller does not have permission to approve or roll back the execution.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            ok: false
            code: ADMIN_FORBIDDEN
            error: "仅平台/站点管理员可审批或回退"
            requestId: "req-20260408-0001"
            details:
              requiredRoles: [platform_admin, site_admin]
              siteId: "btwentyfive"
    ConflictError:
      description: Execution state-machine conflict.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            ok: false
            code: CONFLICT
            error: "Execution must be approved before rollback"
            requestId: "req-20260408-0002"
    NotImplementedDraft:
      description: Optimize draft endpoint returns governance draft details in a 501 envelope.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/OptimizeDraftNotImplementedResponse"
          example:
            ok: false
            code: NOT_IMPLEMENTED
            error: "Optimize engine is not implemented yet; returning a context-backed draft"
            requestId: "req-20260408-0003"
    UpstreamError:
      description: Upstream legacy endpoint failure.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            ok: false
            code: UPSTREAM_ERROR
            error: "Legacy request failed"
            requestId: "req-20260311-0003"
            sourceStatus:
              overall: failed
              sources:
                - key: energy
                  endpoint: "/zsqy/homepage/126lnoffice/getEnergyStatisticsCurve"
                  ok: false
                  status: 500
                  message: null
                  error: "Legacy request failed: 500"
                  rows: null
    InternalError:
      description: BFF internal unhandled error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            ok: false
            code: INTERNAL_ERROR
            error: "Unhandled error"
            requestId: "req-20260311-0004"

  schemas:
    HealthzResponse:
      type: object
      required: [ok, service, port, legacyBaseUrl]
      properties:
        ok:
          type: boolean
        service:
          type: string
        port:
          type: integer
        legacyBaseUrl:
          type: string

    SiteRef:
      type: object
      required: [siteId, siteName]
      properties:
        siteId:
          type: string
        siteName:
          type: string

    Freshness:
      type: object
      description: Unified freshness metadata for all aggregation payloads.
      required: [latestTimestamp, stale, ageHours]
      properties:
        latestTimestamp:
          type: string
          format: date-time
          nullable: true
        stale:
          type: boolean
        ageHours:
          type: number
          format: double
          nullable: true

    SourceEndpointStatus:
      type: object
      required: [key, endpoint, ok, status, message, error, rows]
      properties:
        key:
          type: string
          description: Stable source key used by the BFF contract. Frontend should map localized text by key first (not by message). For metric-level recommendation sources, key must be metric.<snake_case> (for example metric.station_total_power_kw). For non-metric sources, use stable adapter keys such as energyCurve/latestAlarmLog/ruleMetrics. If client receives non-contract payload with missing key, fallback to endpoint only as a temporary UI label and report contract drift.
          pattern: "^(metric\\.[a-z0-9_]+|[A-Za-z][A-Za-z0-9._-]*)$"
          example: metric.station_total_power_kw
        endpoint:
          type: string
        ok:
          type: boolean
        status:
          type: integer
          nullable: true
          description: Upstream HTTP status when available. Use null when upstream is unreachable or no HTTP status is available (for example timeout/network/parser failure). When value is >=500, treat as upstream service failure.
        message:
          type: string
          nullable: true
          description: Optional non-localized adapter detail. Frontend should treat this as secondary detail and avoid direct rendering as the primary localized text.
        error:
          type: string
          nullable: true
        rows:
          type: integer
          nullable: true

    SourceStatus:
      type: object
      description: Unified source status for all aggregation payloads.
      required: [overall, sources]
      properties:
        overall:
          type: string
          enum: [ok, partial, failed]
        sources:
          type: array
          minItems: 0
          description: Per-source status list. Empty array means no upstream source participated for this payload.
          items:
            $ref: "#/components/schemas/SourceEndpointStatus"

    ErrorResponse:
      type: object
      required: [ok, code, error]
      properties:
        ok:
          type: boolean
          enum: [false]
        code:
          type: string
          enum: [BAD_REQUEST, NOT_FOUND, UPSTREAM_ERROR, INTERNAL_ERROR, ADMIN_FORBIDDEN, CONFLICT, NOT_IMPLEMENTED]
        error:
          type: string
        requestId:
          type: string
          nullable: true
        details:
          type: object
          nullable: true
          additionalProperties: true
        freshness:
          allOf:
            - $ref: "#/components/schemas/Freshness"
          nullable: true
        sourceStatus:
          allOf:
            - $ref: "#/components/schemas/SourceStatus"
          nullable: true

    OverviewEnergyCards:
      type: object
      required:
        - currentCop
        - totalPowerKw
        - currentLoadRate
        - totalElectricityKwh
        - savingPotentialPct
        - activeAnomalyCount
        - chilledDeltaT
        - coolingDeltaT
      properties:
        currentCop:
          type: number
          format: double
          nullable: true
        totalPowerKw:
          type: number
          format: double
          nullable: true
        currentLoadRate:
          type: number
          format: double
          nullable: true
        totalElectricityKwh:
          type: number
          format: double
          nullable: true
        savingPotentialPct:
          type: number
          format: double
          nullable: true
        activeAnomalyCount:
          type: integer
          nullable: true
        chilledDeltaT:
          type: number
          format: double
          nullable: true
        coolingDeltaT:
          type: number
          format: double
          nullable: true

    DeviceSummary:
      type: object
      required:
        - totalDevices
        - chillerCount
        - chilledPumpCount
        - coolingPumpCount
        - coolingTowerCount
      properties:
        totalDevices:
          type: integer
        chillerCount:
          type: integer
        chilledPumpCount:
          type: integer
        coolingPumpCount:
          type: integer
        coolingTowerCount:
          type: integer

    AlarmCounts:
      type: object
      required: [total, high, medium, low]
      properties:
        total:
          type: integer
        high:
          type: integer
        medium:
          type: integer
        low:
          type: integer

    AnomalyCounts:
      type: object
      required: [total, critical, major, minor, normal]
      properties:
        total:
          type: integer
        critical:
          type: integer
        major:
          type: integer
        minor:
          type: integer
        normal:
          type: integer

    DashboardOverviewResponse:
      type: object
      required: [site, generatedAt, energyCards, deviceSummary, alarmSummary, freshness, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        energyCards:
          $ref: "#/components/schemas/OverviewEnergyCards"
        deviceSummary:
          allOf:
            - $ref: "#/components/schemas/DeviceSummary"
          nullable: true
        alarmSummary:
          $ref: "#/components/schemas/AlarmCounts"
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    TrendPoint:
      type: object
      required: [t, v]
      properties:
        t:
          type: string
          format: date-time
        v:
          type: number
          format: double
          nullable: true

    TrendSeries:
      type: object
      required: [metric, label, points]
      properties:
        metric:
          type: string
        label:
          type: string
        points:
          type: array
          items:
            $ref: "#/components/schemas/TrendPoint"

    TrendStats:
      type: object
      required: [metric, latest, min, max]
      properties:
        metric:
          type: string
        latest:
          type: number
          format: double
          nullable: true
        min:
          type: number
          format: double
          nullable: true
        max:
          type: number
          format: double
          nullable: true

    DashboardTrendsResponse:
      type: object
      required: [site, generatedAt, range, series, stats, freshness, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        range:
          type: string
          enum: [24h, 7d, 30d]
        series:
          type: array
          items:
            $ref: "#/components/schemas/TrendSeries"
        stats:
          type: array
          items:
            $ref: "#/components/schemas/TrendStats"
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    AnomalyEvent:
      type: object
      required: [id, title, severity, occurredAt, source]
      properties:
        id:
          type: string
        title:
          type: string
        severity:
          type: string
          enum: [critical, major, minor, normal]
        state:
          type: string
          nullable: true
        occurredAt:
          type: string
          format: date-time
          nullable: true
        source:
          type: string
        regId:
          type: string
          nullable: true
        value:
          type: string
          nullable: true

    DiagnosisFlags:
      type: object
      required: [staleAlarmFeed, missingHighSeverity]
      properties:
        staleAlarmFeed:
          type: boolean
        missingHighSeverity:
          type: boolean

    AnomaliesSummaryResponse:
      type: object
      required: [site, generatedAt, counts, latestEvents, diagnosisFlags, freshness, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        counts:
          $ref: "#/components/schemas/AnomalyCounts"
        latestEvents:
          type: array
          items:
            $ref: "#/components/schemas/AnomalyEvent"
        diagnosisFlags:
          $ref: "#/components/schemas/DiagnosisFlags"
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    AnomaliesListResponse:
      type: object
      required: [site, generatedAt, items, page, pageSize, total, filters, freshness, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/AnomalyEvent"
        page:
          type: integer
        pageSize:
          type: integer
        total:
          type: integer
        filters:
          type: object
          required: [severity, state]
          properties:
            severity:
              type: string
              enum: [critical, major, minor, normal]
              nullable: true
            state:
              type: string
              nullable: true
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    RecommendationCard:
      type: object
      required:
        - id
        - title
        - priority
        - category
        - reason
        - evidence
        - actions
        - relatedDevices
        - severity
        - risk
      properties:
        id:
          type: string
        title:
          type: string
        priority:
          type: string
          enum: [high, medium, low]
        category:
          type: string
        reason:
          type: string
        evidence:
          type: array
          items:
            type: string
        actions:
          type: array
          items:
            type: string
        relatedDevices:
          type: array
          items:
            type: string
        severity:
          type: string
          enum: [critical, major, minor]
        risk:
          type: string
          enum: [high, medium, low]
        ruleId:
          type: string
          nullable: true

    RecommendationSummary:
      type: object
      required: [total, highRisk, criticalSeverity]
      properties:
        total:
          type: integer
        highRisk:
          type: integer
        criticalSeverity:
          type: integer

    RuleEvaluation:
      type: object
      required:
        - rulesLoaded
        - error
        - ruleSetId
        - version
        - totalEnabledRules
        - matchedRuleIds
        - skippedRuleIds
        - skippedRuleDetails
        - missingMetricPolicy
      properties:
        rulesLoaded:
          type: boolean
        error:
          type: string
          nullable: true
        ruleSetId:
          type: string
          nullable: true
        version:
          type: string
          nullable: true
        totalEnabledRules:
          type: integer
        matchedRuleIds:
          type: array
          items:
            type: string
        skippedRuleIds:
          type: array
          items:
            type: string
        skippedRuleDetails:
          type: array
          items:
            $ref: "#/components/schemas/SkippedRuleDetail"
        missingMetricPolicy:
          type: string
          nullable: true

    SkippedRuleMissingMetric:
      type: object
      required: [metric, category]
      properties:
        metric:
          type: string
        category:
          type: string
          description: Stable missing-metric category for frontend localization mapping.
          enum: [upstream_unreachable, field_missing_or_invalid, unknown]
        message:
          type: string
          nullable: true
          description: Optional raw detail string. Frontend should render localized copy by category and use message only as extra diagnostic detail.

    SkippedRuleDetail:
      type: object
      required: [ruleId, reason, missingMetrics]
      properties:
        ruleId:
          type: string
        reason:
          type: string
        missingMetrics:
          type: array
          items:
            $ref: "#/components/schemas/SkippedRuleMissingMetric"

    RecommendationsResponse:
      type: object
      required: [site, generatedAt, summary, cards, ruleEvaluation, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        summary:
          $ref: "#/components/schemas/RecommendationSummary"
        cards:
          type: array
          items:
            $ref: "#/components/schemas/RecommendationCard"
        ruleEvaluation:
          $ref: "#/components/schemas/RuleEvaluation"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    TopologyGroup:
      type: object
      required: [id, name, type, floor]
      properties:
        id:
          type: string
        name:
          type: string
        type:
          type: string
        floor:
          type: string

    TopologyNode:
      type: object
      required: [id, label, count, downstream]
      properties:
        id:
          type: string
        label:
          type: string
        count:
          type: integer
          nullable: true
        downstream:
          type: string
          nullable: true

    SystemTopologyResponse:
      type: object
      required: [site, generatedAt, summary, groups, nodes, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        summary:
          $ref: "#/components/schemas/DeviceSummary"
        groups:
          type: array
          items:
            $ref: "#/components/schemas/TopologyGroup"
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/TopologyNode"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    DiagramPositionHint:
      type: object
      required: [row, column, lane, x, y, z, anchor]
      properties:
        row:
          type: integer
        column:
          type: integer
        lane:
          type: string
        x:
          type: number
          format: double
        y:
          type: number
          format: double
        z:
          type: number
          format: double
        anchor:
          type: string
          enum: [left, center, right, top, bottom]

    DiagramRotationHint:
      type: object
      required: [yaw, pitch, roll]
      properties:
        yaw:
          type: number
          format: double
        pitch:
          type: number
          format: double
        roll:
          type: number
          format: double

    DiagramStatusSummary:
      type: object
      required: [runStatus, alarmStatus, degraded, latestUpdateAt]
      properties:
        runStatus:
          type: string
          enum: [run, stop, unknown]
        alarmStatus:
          type: string
          enum: [normal, alarm, unknown]
        degraded:
          type: boolean
        latestUpdateAt:
          type: string
          format: date-time
          nullable: true

    DiagramRoutePoint:
      type: object
      required: [x, y, z]
      properties:
        x:
          type: number
          format: double
        y:
          type: number
          format: double
        z:
          type: number
          format: double

    DiagramRouteHint:
      type: object
      required: [points]
      properties:
        points:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/DiagramRoutePoint"

    SystemDiagramNode:
      type: object
      required:
        [
          id,
          nodeType,
          label,
          systemType,
          role,
          group,
          deviceIdRef,
          deviceIds,
          modelCategory,
          positionHint,
          rotationHint,
          statusSummary
        ]
      properties:
        id:
          type: string
        nodeType:
          type: string
          enum: [device, valve, header, load, sensor, virtual]
        label:
          type: string
        systemType:
          type: string
          enum: [chiller, chilledPump, coolingPump, coolingTower, chilledValve, coolingValve, load, other]
        role:
          type: string
          enum: [main, branch, return, bypass, header]
        group:
          type: string
        deviceIdRef:
          type: string
          nullable: true
        deviceIds:
          type: array
          items:
            type: string
        modelCategory:
          type: string
          enum: [chiller, pump, valve, coolingTower, load, other]
        positionHint:
          $ref: "#/components/schemas/DiagramPositionHint"
        rotationHint:
          $ref: "#/components/schemas/DiagramRotationHint"
        statusSummary:
          $ref: "#/components/schemas/DiagramStatusSummary"

    SystemDiagramEdge:
      type: object
      required: [id, from, to, edgeType, pipeClass, routeHint]
      properties:
        id:
          type: string
        from:
          type: string
        to:
          type: string
        edgeType:
          type: string
          enum: [pipe, control, logical]
        pipeClass:
          type: string
          enum: [chilled_supply, chilled_return, cooling_supply, cooling_return, bypass]
        routeHint:
          $ref: "#/components/schemas/DiagramRouteHint"

    SystemDiagramGroup:
      type: object
      required: [id, label, groupType, nodeIds]
      properties:
        id:
          type: string
        label:
          type: string
        groupType:
          type: string
          enum: [loop, cluster, zone]
        nodeIds:
          type: array
          items:
            type: string

    SystemDiagramStats:
      type: object
      required: [totalNodes, totalEdges, deviceNodeCount, virtualNodeCount, degradedNodeCount]
      properties:
        totalNodes:
          type: integer
        totalEdges:
          type: integer
        deviceNodeCount:
          type: integer
        virtualNodeCount:
          type: integer
        degradedNodeCount:
          type: integer

    SystemDiagramResponse:
      type: object
      required:
        [site, generatedAt, layoutMode, scope, freshness, sourceStatus, nodes, edges, groups, stats]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        layoutMode:
          type: string
          enum: [auto, fixed]
        scope:
          type: string
          enum: [main_loop, full]
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/SystemDiagramNode"
        edges:
          type: array
          items:
            $ref: "#/components/schemas/SystemDiagramEdge"
        groups:
          type: array
          items:
            $ref: "#/components/schemas/SystemDiagramGroup"
        stats:
          $ref: "#/components/schemas/SystemDiagramStats"

    DeviceListItem:
      type: object
      required:
        [deviceId, deviceCode, deviceName, systemType, floorName, buildingName, usageType, iconPath, status, lastReportAt]
      properties:
        deviceId:
          type: string
        deviceCode:
          type: string
        deviceName:
          type: string
        systemType:
          type: string
          enum: [chiller, chilledPump, coolingPump, coolingTower, other]
        floorName:
          type: string
        buildingName:
          type: string
        usageType:
          type: string
        iconPath:
          type: string
          nullable: true
        status:
          type: string
          enum: [online, offline, unknown]
        lastReportAt:
          type: string
          format: date-time
          nullable: true

    DevicesListResponse:
      type: object
      required: [site, generatedAt, items, page, pageSize, total, filters, freshness, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/DeviceListItem"
        page:
          type: integer
        pageSize:
          type: integer
        total:
          type: integer
        filters:
          type: object
          required: [type, floor]
          properties:
            type:
              type: string
              enum: [chiller, chilledPump, coolingPump, coolingTower, other]
              nullable: true
            floor:
              type: string
              nullable: true
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    DeviceTreeNode:
      type: object
      required:
        [id, label, nodeType, parentId, deviceIdRef, deviceCode, deviceName, systemType, floorName, buildingName, status, lastReportAt, childCount, children]
      properties:
        id:
          type: string
        label:
          type: string
        nodeType:
          type: string
          enum: [root, group, device, point]
        parentId:
          type: string
          nullable: true
        deviceIdRef:
          type: string
          nullable: true
        deviceCode:
          type: string
          nullable: true
        deviceName:
          type: string
          nullable: true
        systemType:
          type: string
          enum: [chiller, chilledPump, coolingPump, coolingTower, other]
          nullable: true
        floorName:
          type: string
          nullable: true
        buildingName:
          type: string
          nullable: true
        status:
          type: string
          enum: [online, offline, unknown]
        lastReportAt:
          type: string
          format: date-time
          nullable: true
        childCount:
          type: integer
        children:
          type: array
          items:
            $ref: "#/components/schemas/DeviceTreeNode"

    DevicesTreeResponse:
      type: object
      required: [site, generatedAt, tree, filters, freshness, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        tree:
          $ref: "#/components/schemas/DeviceTreeNode"
          nullable: true
        filters:
          type: object
          required: [build, floor]
          properties:
            build:
              type: integer
            floor:
              type: integer
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    DeviceDetail:
      type: object
      required:
        [deviceId, deviceCode, deviceName, deviceTypeCode, deviceTypeName, floorName, buildingName, usageType, iconPath, isVirtual, runStatusText, alarmStatusText, latestUpdateAt, treeNodeType, treeChildCount, pointCount]
      properties:
        deviceId:
          type: string
        deviceCode:
          type: string
        deviceName:
          type: string
        deviceTypeCode:
          type: string
          nullable: true
        deviceTypeName:
          type: string
          nullable: true
        floorName:
          type: string
        buildingName:
          type: string
        usageType:
          type: string
        iconPath:
          type: string
          nullable: true
        isVirtual:
          type: boolean
          nullable: true
        runStatusText:
          type: string
          nullable: true
        alarmStatusText:
          type: string
          nullable: true
        latestUpdateAt:
          type: string
          format: date-time
          nullable: true
        treeNodeType:
          type: string
          enum: [root, group, device, point]
          nullable: true
        treeChildCount:
          type: integer
          nullable: true
        pointCount:
          type: integer
          nullable: true

    DeviceDetailResponse:
      type: object
      required: [site, generatedAt, detail, freshness, sourceStatus]
      properties:
        site:
          $ref: "#/components/schemas/SiteRef"
        generatedAt:
          type: string
          format: date-time
        detail:
          allOf:
            - $ref: "#/components/schemas/DeviceDetail"
          nullable: true
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"

    OptimizeExecutionSiteRef:
      type: object
      required: [siteId]
      properties:
        siteId:
          type: string

    OptimizeExecutionTimelineEntry:
      type: object
      required: [at, actorUserId, action, note]
      properties:
        at:
          type: string
          format: date-time
        actorUserId:
          type: string
        action:
          type: string
          enum: [created, approved, rolled_back]
        note:
          type: string
          nullable: true

    OptimizeExecutionDraft:
      type: object
      required: [generatedAt, gateLevel]
      properties:
        generatedAt:
          type: string
          format: date-time
          nullable: true
        gateLevel:
          type: string
          nullable: true

    OptimizeExecutionPlan:
      type: object
      required: [type, schemeKey, title, targetCop, targetPowerKw, targetApproachC, targetTcwsC, reason, actions]
      properties:
        type:
          type: string
          enum: [scheme, tower-approach, pump-delta-t]
        schemeKey:
          type: string
          nullable: true
        title:
          type: string
          nullable: true
        targetCop:
          type: number
          format: double
          nullable: true
        targetPowerKw:
          type: number
          format: double
          nullable: true
        targetApproachC:
          type: number
          format: double
          nullable: true
        targetTcwsC:
          type: number
          format: double
          nullable: true
        targetChwpFreqTrimHz:
          type: number
          format: double
          nullable: true
          description: AI_ChwpFreqTrim_Hz value for pump-delta-t executions. Range -5 to +3 Hz.
        targetCwpFreqTrimHz:
          type: number
          format: double
          nullable: true
          description: AI_CwpFreqTrim_Hz value for pump-delta-t executions. Range -5 to +3 Hz.
        ttlSeconds:
          type: integer
          nullable: true
        holdMinutes:
          type: integer
          nullable: true
        rollbackLockoutMinutes:
          type: integer
          nullable: true
        targetPoints:
          type: object
          nullable: true
          additionalProperties: true
        reason:
          type: string
          nullable: true
        actions:
          type: array
          items:
            type: string

    OptimizeExecutionApprovalState:
      type: object
      required: [required, status, requestedAt, approvedAt, approverUserId, note]
      properties:
        required:
          type: boolean
        status:
          type: string
          enum: [pending, approved]
        requestedAt:
          type: string
          format: date-time
          nullable: true
        approvedAt:
          type: string
          format: date-time
          nullable: true
        approverUserId:
          type: string
          nullable: true
        note:
          type: string
          nullable: true

    OptimizeExecutionRollbackState:
      type: object
      required: [status, reason, requestedBy, requestedAt, completedAt]
      properties:
        status:
          type: string
          enum: [none, completed]
        reason:
          type: string
          nullable: true
        requestedBy:
          type: string
          nullable: true
        requestedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true

    OptimizeExecutionRecord:
      type: object
      required:
        - executionId
        - siteId
        - createdAt
        - updatedAt
        - approvedAt
        - approvedBy
        - rolledBackAt
        - rolledBackBy
        - status
        - draft
        - execution
        - approval
        - rollback
        - timeline
      properties:
        executionId:
          type: string
        siteId:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        approvedAt:
          type: string
          format: date-time
          nullable: true
        approvedBy:
          type: string
          nullable: true
        rolledBackAt:
          type: string
          format: date-time
          nullable: true
        rolledBackBy:
          type: string
          nullable: true
        status:
          type: string
          enum: [pending_approval, approved, rolled_back]
        draft:
          $ref: "#/components/schemas/OptimizeExecutionDraft"
        execution:
          $ref: "#/components/schemas/OptimizeExecutionPlan"
        approval:
          $ref: "#/components/schemas/OptimizeExecutionApprovalState"
        rollback:
          $ref: "#/components/schemas/OptimizeExecutionRollbackState"
        timeline:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeExecutionTimelineEntry"

    OptimizeExecutionListResponse:
      type: object
      required: [site, generatedAt, total, items]
      properties:
        site:
          $ref: "#/components/schemas/OptimizeExecutionSiteRef"
        generatedAt:
          type: string
          format: date-time
        total:
          type: integer
        items:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeExecutionRecord"

    OptimizeExecutionMutationResponse:
      type: object
      required: [ok, requestId, execution]
      properties:
        ok:
          type: boolean
          enum: [true]
        requestId:
          type: string
        execution:
          $ref: "#/components/schemas/OptimizeExecutionRecord"

    CreateOptimizeExecutionRequest:
      type: object
      required: [execution]
      properties:
        draft:
          type: object
          properties:
            generatedAt:
              type: string
              format: date-time
              nullable: true
            gateLevel:
              type: string
              nullable: true
        execution:
          type: object
          description: |
            When type is `scheme`, provide either `schemeKey` or `title`.
            Missing `type` defaults to `scheme`.
            When type is `pump-delta-t`, provide `targetChwpFreqTrimHz` or `targetCwpFreqTrimHz`.
          properties:
            type:
              type: string
              enum: [scheme, tower-approach, pump-delta-t]
              default: scheme
            schemeKey:
              type: string
              nullable: true
            title:
              type: string
              nullable: true
            targetCop:
              type: number
              format: double
              nullable: true
            targetPowerKw:
              type: number
              format: double
              nullable: true
            targetApproachC:
              type: number
              format: double
              nullable: true
            targetTcwsC:
              type: number
              format: double
              nullable: true
            targetChwpFreqTrimHz:
              type: number
              format: double
              nullable: true
              minimum: -5
              maximum: 3
            targetCwpFreqTrimHz:
              type: number
              format: double
              nullable: true
              minimum: -5
              maximum: 3
            ttlSeconds:
              type: integer
              nullable: true
            holdMinutes:
              type: integer
              nullable: true
            rollbackLockoutMinutes:
              type: integer
              nullable: true
            targetPoints:
              type: object
              nullable: true
              additionalProperties: true
            reason:
              type: string
              nullable: true
            actions:
              type: array
              items:
                type: string
        approval:
          type: object
          properties:
            required:
              type: boolean
              default: true
            note:
              type: string
              nullable: true

    ApproveOptimizeExecutionRequest:
      type: object
      properties:
        note:
          type: string
          nullable: true
        approverUserId:
          type: string
          nullable: true

    RollbackOptimizeExecutionRequest:
      type: object
      properties:
        reason:
          type: string
          nullable: true

    OptimizeDraftRequest:
      type: object
      properties:
        context:
          type: object
          properties:
            siteId:
              type: string
              nullable: true
        inputs:
          type: object
          properties:
            loadKw:
              type: number
              format: double
            outdoorTempC:
              type: number
              format: double
            mode:
              type: string
              enum: [cooling]
              default: cooling
            equipmentContext:
              type: object
              properties:
                activeChillerIds:
                  type: array
                  items:
                    type: string
                activeChillerModels:
                  type: array
                  items:
                    type: string

    OptimizeDraftDecision:
      type: object
      properties:
        summary:
          type: string
        confidence:
          type: string

    OptimizeDraftScenario:
      type: object
      properties:
        loadKw:
          type: number
          format: double
        outdoorTempC:
          type: number
          format: double
        mode:
          type: string

    OptimizeDraftRecommendation:
      type: object
      properties:
        systemCop:
          type: number
          format: double
          nullable: true
        totalPowerKw:
          type: number
          format: double
          nullable: true
        steps:
          type: array
          items:
            type: string

    OptimizeDraftGate:
      type: object
      properties:
        level:
          type: string
          enum: [ready, caution, blocked]
        title:
          type: string
        reason:
          type: string

    OptimizeDraftBaseline:
      type: object
      properties:
        systemCop:
          type: number
          format: double
          nullable: true
        totalPowerKw:
          type: number
          format: double
          nullable: true
        chilledDeltaT:
          type: number
          format: double
          nullable: true
        coolingDeltaT:
          type: number
          format: double
          nullable: true
        chillerPowerKw:
          type: number
          format: double
          nullable: true
        chilledPumpPowerKw:
          type: number
          format: double
          nullable: true
        coolingPumpPowerKw:
          type: number
          format: double
          nullable: true
        coolingTowerPowerKw:
          type: number
          format: double
          nullable: true
        chilledSupplyTemp:
          type: number
          format: double
          nullable: true
        coolingReturnTemp:
          type: number
          format: double
          nullable: true
        thermalUnbalanceRate:
          type: number
          format: double
          nullable: true
        activeAlarmCount:
          type: integer
          nullable: true

    OptimizeDraftActionLink:
      type: object
      properties:
        href:
          type: string
        enabled:
          type: boolean
        label:
          type: string

    OptimizeDraftHistoryBenchmarkWindow:
      type: object
      properties:
        defaultMonths:
          type: array
          items:
            type: string
        months:
          type: array
          items:
            type: string
        maxMonths:
          type: integer
          nullable: true

    OptimizeDraftReferenceCop:
      type: object
      properties:
        low:
          type: number
          format: double
          nullable: true
        median:
          type: number
          format: double
          nullable: true
        high:
          type: number
          format: double
          nullable: true

    OptimizeDraftCurrentGap:
      type: object
      properties:
        copDeltaToMedian:
          type: number
          format: double
          nullable: true
        powerDeltaKwToMedian:
          type: number
          format: double
          nullable: true

    OptimizeDraftHistoryBenchmarkLinks:
      type: object
      properties:
        proportionHref:
          $ref: "#/components/schemas/OptimizeDraftActionLink"
        compareHref:
          $ref: "#/components/schemas/OptimizeDraftActionLink"

    OptimizeDraftHistoryBenchmark:
      type: object
      properties:
        status:
          type: string
          enum: [ready, partial, unavailable]
        mode:
          type: string
        ratedCoolingCapacityKw:
          type: number
          format: double
          nullable: true
        requestedLoadRatePct:
          type: number
          format: double
          nullable: true
        matchedBucketLabel:
          type: string
          nullable: true
        matchingTier:
          type: string
          enum: [load-wetbulb-strict, load-wetbulb-relaxed, load-only-fallback, unavailable]
        fallbackLevel:
          type: integer
          nullable: true
        requestedWetBulbC:
          type: number
          format: double
          nullable: true
        matchedWetBulbBand:
          type: string
          nullable: true
        matchedWetBulbBands:
          type: array
          items:
            type: string
        wetBulbToleranceC:
          type: number
          format: double
          nullable: true
        confidence:
          type: string
          enum: [high, medium, low]
        sampleWindow:
          $ref: "#/components/schemas/OptimizeDraftHistoryBenchmarkWindow"
        sampleCount:
          type: integer
          nullable: true
        matchedMonthCount:
          type: integer
          nullable: true
        referenceCop:
          $ref: "#/components/schemas/OptimizeDraftReferenceCop"
        currentGap:
          $ref: "#/components/schemas/OptimizeDraftCurrentGap"
        note:
          type: string
          nullable: true
        availabilityReasonCode:
          type: string
          nullable: true
        links:
          $ref: "#/components/schemas/OptimizeDraftHistoryBenchmarkLinks"

    OptimizeDraftBenefitBasis:
      type: object
      properties:
        matchingTier:
          type: string
        fallbackLevel:
          type: integer
          nullable: true
        sampleCount:
          type: integer
          nullable: true
        monthCount:
          type: integer
          nullable: true
        matchedMonthCount:
          type: integer
          nullable: true
        matchedWetBulbBand:
          type: string
          nullable: true
        matchedWetBulbBands:
          type: array
          items:
            type: string
        requestedWetBulbC:
          type: number
          format: double
          nullable: true
        wetBulbToleranceC:
          type: number
          format: double
          nullable: true

    OptimizeDraftBenefitEstimate:
      type: object
      properties:
        status:
          type: string
          enum: [ready, partial, unavailable]
        opportunityLevel:
          type: string
          enum: [low, medium, high]
        confidence:
          type: string
          enum: [high, medium, low]
        expectedTargetCop:
          type: number
          format: double
          nullable: true
        expectedTargetPowerKw:
          type: number
          format: double
          nullable: true
        expectedPowerDeltaKw:
          type: number
          format: double
          nullable: true
        expectedPowerDeltaPct:
          type: number
          format: double
          nullable: true
        expectedCopDelta:
          type: number
          format: double
          nullable: true
        basis:
          $ref: "#/components/schemas/OptimizeDraftBenefitBasis"
        disclaimer:
          type: string
          nullable: true

    OptimizeDraftTowerTargetBand:
      type: object
      properties:
        low:
          type: number
          format: double
          nullable: true
        high:
          type: number
          format: double
          nullable: true
        label:
          type: string
          nullable: true

    OptimizeDraftTowerGuardrail:
      type: object
      properties:
        key:
          type: string
          nullable: true
        status:
          type: string
          enum: [ready, missing, blocked, locked]
        value:
          nullable: true
          oneOf:
            - type: string
            - type: number
            - type: boolean
        message:
          type: string
          nullable: true
        resolvedBy:
          type: string
          nullable: true
        matchedKeys:
          type: array
          items:
            type: string

    OptimizeDraftInputSignal:
      type: object
      properties:
        key:
          type: string
        label:
          type: string
        value:
          nullable: true
          oneOf:
            - type: string
            - type: number
            - type: boolean
        unit:
          type: string
          nullable: true
        required:
          type: boolean
        ok:
          type: boolean
        reason:
          type: string
          nullable: true

    OptimizeDraftTowerApproachAdvisor:
      type: object
      properties:
        status:
          type: string
          enum: [ready, partial, unavailable]
        currentApproachC:
          type: number
          format: double
          nullable: true
        currentTcwsC:
          type: number
          format: double
          nullable: true
        requestedWetBulbC:
          type: number
          format: double
          nullable: true
        targetApproachC:
          type: number
          format: double
          nullable: true
        targetTcwsC:
          type: number
          format: double
          nullable: true
        targetBandC:
          $ref: "#/components/schemas/OptimizeDraftTowerTargetBand"
        executionReady:
          type: boolean
        controlMode:
          type: string
        reason:
          type: string
          nullable: true
        disclaimer:
          type: string
          nullable: true
        guardrails:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftTowerGuardrail"

    OptimizeDraftPumpDeltaTAdvisor:
      type: object
      properties:
        status:
          type: string
          enum: [ready, partial, unavailable]
        executionReady:
          type: boolean
        dispatchReady:
          type: boolean
        executionMode:
          type: string
          enum: [read_only, shadow, assisted]
        dispatchMode:
          type: string
          enum: [off, shadow, assisted]
        controlMode:
          type: string
        current:
          type: object
          additionalProperties:
            type: number
            format: double
            nullable: true
        targetPoints:
          type: object
          properties:
            chilledPump:
              type: string
              example: AI_ChwpFreqTrim_Hz
            coolingPump:
              type: string
              example: AI_CwpFreqTrim_Hz
            unit:
              type: string
              example: Hz
            ttlSeconds:
              type: integer
              example: 300
            trimRangeHz:
              type: object
              properties:
                min:
                  type: number
                  example: -5
                max:
                  type: number
                  example: 3
        targetBandsC:
          type: object
          additionalProperties: true
        outputTargets:
          type: object
          properties:
            chilledPumpFreqTrimHz:
              type: number
              format: double
              nullable: true
            coolingPumpFreqTrimHz:
              type: number
              format: double
              nullable: true
            rollbackTrimHz:
              type: number
              format: double
              nullable: true
            maxStepHz:
              type: number
              format: double
              nullable: true
            ttlSeconds:
              type: integer
              nullable: true
            holdMinutes:
              type: integer
              nullable: true
            rollbackLockoutMinutes:
              type: integer
              nullable: true
            trimRangeHz:
              type: object
              additionalProperties: true
        reason:
          type: string
          nullable: true
        disclaimer:
          type: string
          nullable: true
        blockers:
          type: array
          items:
            type: string
        warnings:
          type: array
          items:
            type: string
        inputSignals:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftInputSignal"
        guardrails:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftTowerGuardrail"
        objective:
          type: object
          additionalProperties: true
        constraints:
          type: object
          additionalProperties: true
        advisorResult:
          type: object
          additionalProperties: true

    OptimizeDraftChillerStagingAdvisor:
      type: object
      properties:
        status:
          type: string
          enum: [ready, partial, unavailable]
        executionReady:
          type: boolean
        dispatchReady:
          type: boolean
        executionMode:
          type: string
          enum: [read_only, shadow]
        dispatchMode:
          type: string
          enum: [off, shadow]
        basis:
          type: string
          example: combination_empirical_performance
        confidence:
          type: string
          enum: [high, medium, low]
        current:
          type: object
          additionalProperties: true
        recommendation:
          type: object
          additionalProperties: true
        evidence:
          type: object
          additionalProperties: true
          description: Combination sample summary, learning boundary, and shadow verification plan.
        sampleEvidence:
          type: object
          additionalProperties: true
          description: Alias of evidence for UI clients that present sample evidence explicitly.
        sampleCapture:
          type: object
          additionalProperties: true
          description: Append-only current live sample capture status. Sample capture never triggers PLC or chiller dispatch.
        candidates:
          type: array
          items:
            type: object
            additionalProperties: true
        savingsVerification:
          type: object
          additionalProperties: true
        blockers:
          type: array
          items:
            type: string
        warnings:
          type: array
          items:
            type: string
        reason:
          type: string
          nullable: true
        disclaimer:
          type: string
          nullable: true
        advisorResult:
          type: object
          additionalProperties: true

    OptimizeDraftReviewReadiness:
      type: object
      properties:
        status:
          type: string
          enum: [ready, partial, unavailable]
        score:
          type: integer
          nullable: true
        reason:
          type: string
          nullable: true
        missingSignals:
          type: array
          items:
            type: string
        hints:
          type: array
          items:
            type: string

    ChillerStagingSample:
      type: object
      properties:
        sampleId:
          type: string
        siteId:
          type: string
        combinationKey:
          type: string
          example: CH4+CH5+CH7
        combination:
          type: array
          items:
            type: string
        capturedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        source:
          type: string
        loadKw:
          type: number
          nullable: true
        loadRatePct:
          type: number
          nullable: true
        wetBulbC:
          type: number
          nullable: true
        stationPowerKw:
          type: number
          nullable: true
        chillerPowerKw:
          type: number
          nullable: true
        stationCop:
          type: number
          nullable: true
        comboCop:
          type: number
          nullable: true
        kwPerRt:
          type: number
          nullable: true
        sampleMinutes:
          type: number
          nullable: true
        alarmCount:
          type: integer
          nullable: true
        sample:
          type: object
          additionalProperties: true

    ChillerStagingSampleListResponse:
      type: object
      required: [site, generatedAt, basis, total, items]
      properties:
        site:
          type: object
          properties:
            siteId:
              type: string
        generatedAt:
          type: string
          format: date-time
        basis:
          type: string
          enum: [append_only_chiller_staging_samples]
        total:
          type: integer
        items:
          type: array
          items:
            $ref: "#/components/schemas/ChillerStagingSample"

    OptimizeDraftDeviceAction:
      type: object
      properties:
        system:
          type: string
        action:
          type: string
        target:
          type: string
          nullable: true
        reason:
          type: string
          nullable: true
        risk:
          type: string
          enum: [low, medium, high]
        priority:
          type: string
          enum: [high, medium, low]
        preconditions:
          type: array
          items:
            type: string

    OptimizeDraftSchemeReadiness:
      type: object
      properties:
        score:
          type: integer
          nullable: true
        level:
          type: string
          enum: [ready, caution, blocked]
        reason:
          type: string
          nullable: true
        blockers:
          type: array
          items:
            type: string
        checkpoints:
          type: array
          items:
            type: string

    OptimizeDraftSchemeLinks:
      type: object
      properties:
        devices:
          $ref: "#/components/schemas/OptimizeDraftActionLink"
        trends:
          $ref: "#/components/schemas/OptimizeDraftActionLink"
        scene:
          $ref: "#/components/schemas/OptimizeDraftActionLink"

    OptimizeDraftScheme:
      type: object
      properties:
        key:
          type: string
        title:
          type: string
        status:
          type: string
          enum: [ready, caution, blocked]
        riskLevel:
          type: string
          enum: [low, medium, high]
        focus:
          type: string
          nullable: true
        label:
          type: string
          nullable: true
        targetPowerKw:
          type: number
          format: double
          nullable: true
        targetCop:
          type: number
          format: double
          nullable: true
        powerDeltaPct:
          type: number
          format: double
          nullable: true
        copDelta:
          type: number
          format: double
          nullable: true
        deviceActions:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftDeviceAction"
        readiness:
          $ref: "#/components/schemas/OptimizeDraftSchemeReadiness"
        actions:
          type: array
          items:
            type: string
        links:
          $ref: "#/components/schemas/OptimizeDraftSchemeLinks"

    OptimizeDraftMissingMetric:
      type: object
      properties:
        metric:
          type: string
        category:
          type: string
          enum: [upstream_unreachable, field_missing_or_invalid, unknown]
        message:
          type: string
          nullable: true

    OptimizeDraftSkippedRule:
      type: object
      properties:
        ruleId:
          type: string
        reason:
          type: string
        missingMetrics:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftMissingMetric"

    OptimizeDraftPrimaryCard:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        priority:
          type: string
          enum: [high, medium, low]
        category:
          type: string
          nullable: true
        reason:
          type: string
          nullable: true
        actions:
          type: array
          items:
            type: string
        evidence:
          type: array
          items:
            type: string
        severity:
          type: string
          nullable: true
        risk:
          type: string
          nullable: true
        relatedDevices:
          type: array
          items:
            type: string
        ruleId:
          type: string
          nullable: true

    OptimizeDraftRuleEvidence:
      type: object
      properties:
        matchedCount:
          type: integer
        skippedCount:
          type: integer
        matchedRuleIds:
          type: array
          items:
            type: string
        skippedRules:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftSkippedRule"
        primaryCards:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftPrimaryCard"

    OptimizeDraftExecutionHub:
      type: object
      properties:
        status:
          type: string
          enum: [idle, pending]
        approvalRequired:
          type: boolean
        endpoint:
          type: string
        pendingApprovalCount:
          type: integer
          nullable: true
        approvedCount:
          type: integer
          nullable: true
        rolledBackCount:
          type: integer
          nullable: true
        latestExecutionId:
          type: string
          nullable: true
        latestExecutionStatus:
          type: string
          nullable: true
        latestExecutionAt:
          type: string
          nullable: true
        note:
          type: string
          nullable: true

    OptimizeDraftDetails:
      type: object
      properties:
        site:
          $ref: "#/components/schemas/OptimizeExecutionSiteRef"
        decision:
          $ref: "#/components/schemas/OptimizeDraftDecision"
        request:
          $ref: "#/components/schemas/OptimizeDraftScenario"
        recommendation:
          $ref: "#/components/schemas/OptimizeDraftRecommendation"
        gate:
          $ref: "#/components/schemas/OptimizeDraftGate"
        baseline:
          $ref: "#/components/schemas/OptimizeDraftBaseline"
        historyBenchmark:
          $ref: "#/components/schemas/OptimizeDraftHistoryBenchmark"
        benefitEstimate:
          $ref: "#/components/schemas/OptimizeDraftBenefitEstimate"
        towerApproachAdvisor:
          $ref: "#/components/schemas/OptimizeDraftTowerApproachAdvisor"
        pumpDeltaTAdvisor:
          $ref: "#/components/schemas/OptimizeDraftPumpDeltaTAdvisor"
        chillerStagingAdvisor:
          $ref: "#/components/schemas/OptimizeDraftChillerStagingAdvisor"
        reviewReadiness:
          $ref: "#/components/schemas/OptimizeDraftReviewReadiness"
        executionHub:
          $ref: "#/components/schemas/OptimizeDraftExecutionHub"
        schemes:
          type: array
          items:
            $ref: "#/components/schemas/OptimizeDraftScheme"
        ruleEvidence:
          $ref: "#/components/schemas/OptimizeDraftRuleEvidence"
        draftLabel:
          type: string
        freshness:
          $ref: "#/components/schemas/Freshness"
        sourceStatus:
          $ref: "#/components/schemas/SourceStatus"
        diagnostics:
          type: array
          items:
            type: string
        generatedAt:
          type: string
          format: date-time

    OptimizeDraftNotImplementedResponse:
      type: object
      required: [ok, code, error, requestId, details]
      properties:
        ok:
          type: boolean
          enum: [false]
        code:
          type: string
          enum: [NOT_IMPLEMENTED]
        error:
          type: string
        requestId:
          type: string
        details:
          $ref: "#/components/schemas/OptimizeDraftDetails"

    OptimizeAdvisorResponse:
      type: object
      required: [ok, code, requestId, details]
      properties:
        ok:
          type: boolean
          enum: [true]
        code:
          type: string
          enum: [OK]
        message:
          type: string
        requestId:
          type: string
        details:
          $ref: "#/components/schemas/OptimizeDraftDetails"
